局部变量是存在于栈区的，程序开始时才分配内存空间，函数运行结束后，为它所分配的空间会被自动释放，函数内的局部变量是有生命周期的。
静态存储区：内存在编译的时候就已经确定好，这块内存在程序整个运行期间都存在。主要存放静态数据、全局数据、常量。
局部变量运行时从栈区分配，而栈区空间是有限的，超出最大限制就报错。
静态变量就是在普通变量前加上static修饰字。这个变量在函数中声明的只能在这个函数中使用，在其他函数中不能使用修改。
动态内存的区域叫做堆区，它使用malloc函数申请，使用free函数释放空间。
